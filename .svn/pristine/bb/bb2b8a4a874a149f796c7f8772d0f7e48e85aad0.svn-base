<?php

echo announcementRemove(trim($_REQUEST['num']));

function announcementRemove($num){
	$user = getUser();
    $a = explode(",",$user->announcement);
    foreach($a as $b){
        if($b == $num)
            unset($b);
    }
	$user->announcement = implode(",", $a);
	$user->save();
}
